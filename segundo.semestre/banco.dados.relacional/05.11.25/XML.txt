COMANDOS SQL PLUS

--CRIAR ARQUIVO DE SPOOL CHAMADO BDR12112025
	SPOOL C:BDR/BDR12112025.TXT

--LINHA DE COMENTARIO
--CONECTAR COMO DBA
	CONNECT / AS SYSDBA

--EXIBIR USUARIO LOGADO
	SHOW USER

--DESBLOQUEAR USUARIO HR COLOCANDO A SENHA HR
	ALTER USER HR INDENTIFIED BY HR ACCOUNT UNLOCK; 

	SHOW USER

--CONECTAR COMO HR

	CONNECT HR/HR

	SHOW USER

	CONNECT / AS SYSDBA

--EXIBIR TODOS OS PARAMETROS DE CONFIGURACAO DO DB

	SHOW PARAMETERS

-- FINALIZAR O DB

	SHUTDOWN

--INICIAR O DB
	STARTUP

--FINALIZAR O DB AGORA! (CUIDADO COM ESSE USO)
	SHUTDOWN IMMEDIATE

--INICIAR O DB SEM ABRIR
	STARTUP MOUNT

--HABILITAR O PACOTE UTL_FILE_DIR
	ALTER SYSTEM SET UTL_FILE_DIR = '*' SCOPE=SPFILE;

--EXIBIR O PARAMETRO UTL_FILE_DIR 
	SHOW PARAMETER UTL_FILE_DIR 

